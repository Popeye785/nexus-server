# 🔍 Claude Audit Report

> Generated by **[claude-audit](https://github.com/itsmesherry/claude-audit)** · 26.5.2026, 13:38:54

## 📋 Project Overview

| Property | Value |
|----------|-------|
| **Project** | `NEXUS_CLEAN` |
| **Languages** | Markdown (116), JSON (21), Shell (20), JavaScript (66), Python (11) |
| **Frameworks** | Express |
| **Files Scanned** | 234 |
| **Total Lines** | 47.549 |
| **Has Tests** | ✅ Yes |
| **Package Manager** | npm |
| **Analysis Type** | ⚡ Static Analysis |
| **Duration** | 125ms |

## 📊 Overall Score

### ❌ Grade: **D** · Score: **45/100**

```
Overall  █████░░░░░ 45/100
```

### Findings Summary

| Severity | Count |
|----------|-------|
| 🔴 Critical | **0** |
| 🟠 High | **15** |
| 🟡 Medium | **11** |
| 🔵 Low | **10** |
| **Total** | **36** |

## 📈 Category Breakdown

```
🔒 Security        ░░░░░░░░░░ 0/100  [F]
📊 Quality         █░░░░░░░░░ 8/100  [F]
⚡ Performance     ████████░░ 80/100  [B]
🏗️ Architecture    ████████░░ 80/100  [B]
📦 Dependencies    ████████░░ 80/100  [B]
🧪 Testing         ████████░░ 75/100  [B]
📚 Documentation   ███████░░░ 72/100  [C]
```

## 🔎 Detailed Findings

### 🔒 Security — Score: 0/100 [F]

#### 🟠 eval() Usage

**Severity:** `HIGH`

Potential eval() Usage found in source code.

**File:** `scripts/train_lstm.py:152`

```
model.eval()
```

**Fix:** Avoid eval(). Use JSON.parse() for JSON, or restructure to avoid dynamic code execution.

---

#### 🟠 eval() Usage

**Severity:** `HIGH`

Potential eval() Usage found in source code.

**File:** `scripts/train_lstm.py:186`

```
model.eval()
```

**Fix:** Avoid eval(). Use JSON.parse() for JSON, or restructure to avoid dynamic code execution.

---

#### 🟠 eval() Usage

**Severity:** `HIGH`

Potential eval() Usage found in source code.

**File:** `scripts/train_lstm.py:215`

```
model.cpu().eval()
```

**Fix:** Avoid eval(). Use JSON.parse() for JSON, or restructure to avoid dynamic code execution.

---

#### 🟠 eval() Usage

**Severity:** `HIGH`

Potential eval() Usage found in source code.

**File:** `scripts/train_lstm_v4.py:238`

```
model.eval()
```

**Fix:** Avoid eval(). Use JSON.parse() for JSON, or restructure to avoid dynamic code execution.

---

#### 🟠 eval() Usage

**Severity:** `HIGH`

Potential eval() Usage found in source code.

**File:** `scripts/train_lstm_v4.py:273`

```
model.eval()
```

**Fix:** Avoid eval(). Use JSON.parse() for JSON, or restructure to avoid dynamic code execution.

---

#### 🟠 eval() Usage

**Severity:** `HIGH`

Potential eval() Usage found in source code.

**File:** `scripts/train_lstm_v4.py:318`

```
model.cpu().eval()
```

**Fix:** Avoid eval(). Use JSON.parse() for JSON, or restructure to avoid dynamic code execution.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation.py:11`

```
decisions = conn.execute(f"""SELECT ts, symbol, decision, confidence FROM aladdin_decisions
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation.py:13`

```
news = conn.execute(f"""SELECT ts, sentiment_score, source_weight FROM news_enriched
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation.py:15`

```
fng = conn.execute(f"""SELECT ts, value FROM fear_greed_history
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation.py:17`

```
funding = conn.execute(f"""SELECT ts, symbol, funding FROM funding_oi_history
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation_v2.py:14`

```
decisions = conn.execute(f"""SELECT ts, decision, confidence FROM aladdin_decisions
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation_v2.py:16`

```
news = conn.execute(f"""SELECT ts, sentiment_score, source_weight FROM news_enriched
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation_v2.py:18`

```
fng = conn.execute(f"""SELECT ts, value FROM fear_greed_history
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---

#### 🟠 SQL String Concatenation (Injection Risk)

**Severity:** `HIGH`

Potential SQL String Concatenation (Injection Risk) found in source code.

**File:** `scripts/walkforward_validation_v2.py:20`

```
funding = conn.execute(f"""SELECT ts, funding FROM funding_oi_history
```

**Fix:** Use parameterized queries or an ORM. Never concatenate user input into SQL strings.

---


### 📊 Quality — Score: 8/100 [F]

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/backtest_engine.js:222 — nesting depth of 6. This hurts readability and testability.

**File:** `modules/backtest_engine.js:222`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/brain_input_shadow.js:119 — nesting depth of 6. This hurts readability and testability.

**File:** `modules/brain_input_shadow.js:119`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟠 Deep Nesting Detected

**Severity:** `HIGH`

modules/crash_recovery_handler.js:96 — nesting depth of 8. This hurts readability and testability.

**File:** `modules/crash_recovery_handler.js:96`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/datasource_macro_calendar.js:65 — nesting depth of 7. This hurts readability and testability.

**File:** `modules/datasource_macro_calendar.js:65`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🔵 Excessive console.log Usage

**Severity:** `LOW`

modules/diagnostics_news_classify.js contains 6 console.log calls. These should not reach production.

**File:** `modules/diagnostics_news_classify.js`

**Fix:** Replace with a proper logger (winston, pino) and remove debug logs before shipping.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/hrp.js:84 — nesting depth of 7. This hurts readability and testability.

**File:** `modules/hrp.js:84`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/hrp_allocator.js:153 — nesting depth of 7. This hurts readability and testability.

**File:** `modules/hrp_allocator.js:153`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/opportunity_scanner.js:190 — nesting depth of 6. This hurts readability and testability.

**File:** `modules/opportunity_scanner.js:190`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/shadow_inference.js:51 — nesting depth of 7. This hurts readability and testability.

**File:** `modules/shadow_inference.js:51`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/slot_strength_ranker.js:71 — nesting depth of 6. This hurts readability and testability.

**File:** `modules/slot_strength_ranker.js:71`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Deep Nesting Detected

**Severity:** `MEDIUM`

modules/squeeze_watcher.js:137 — nesting depth of 7. This hurts readability and testability.

**File:** `modules/squeeze_watcher.js:137`

**Fix:** Use early returns, extract functions, or restructure logic to reduce nesting. Aim for max depth of 3.

---

#### 🟡 Large File — Consider Splitting

**Severity:** `MEDIUM`

public/translations.js has 532 lines. Large files are hard to test, review, and maintain.

**File:** `public/translations.js`

**Fix:** Split into smaller, focused modules. Follow the Single Responsibility Principle.

---

#### 🔵 Excessive console.log Usage

**Severity:** `LOW`

scripts/backfill_etf_sosovalue.js contains 6 console.log calls. These should not reach production.

**File:** `scripts/backfill_etf_sosovalue.js`

**Fix:** Replace with a proper logger (winston, pino) and remove debug logs before shipping.

---

#### 🔵 Excessive console.log Usage

**Severity:** `LOW`

scripts/backfill_news_enriched.js contains 10 console.log calls. These should not reach production.

**File:** `scripts/backfill_news_enriched.js`

**Fix:** Replace with a proper logger (winston, pino) and remove debug logs before shipping.

---

#### 🔵 Excessive console.log Usage

**Severity:** `LOW`

scripts/dca_stranded_cleanup.js contains 14 console.log calls. These should not reach production.

**File:** `scripts/dca_stranded_cleanup.js`

**Fix:** Replace with a proper logger (winston, pino) and remove debug logs before shipping.

---

#### 🔵 Excessive console.log Usage

**Severity:** `LOW`

scripts/sub_bot.js contains 6 console.log calls. These should not reach production.

**File:** `scripts/sub_bot.js`

**Fix:** Replace with a proper logger (winston, pino) and remove debug logs before shipping.

---

#### 🔵 Excessive console.log Usage

**Severity:** `LOW`

tests/smoke.spec.js contains 7 console.log calls. These should not reach production.

**File:** `tests/smoke.spec.js`

**Fix:** Replace with a proper logger (winston, pino) and remove debug logs before shipping.

---


### ⚡ Performance — Score: 80/100 [B]

✅ No issues found in this category.

### 🏗️ Architecture — Score: 80/100 [B]

✅ No issues found in this category.

### 📦 Dependencies — Score: 80/100 [B]

✅ No issues found in this category.

### 🧪 Testing — Score: 75/100 [B]

#### 🟡 Low Test Coverage

**Severity:** `MEDIUM`

Only 5% of source files have corresponding tests (4 test files for 77 source files).

**Fix:** Add unit tests for critical business logic. Aim for at least 70% code coverage.

---


### 📚 Documentation — Score: 72/100 [C]

#### 🔵 Insufficient Documentation

**Severity:** `LOW`

modules/ccxt_exchanges.js has < 5% comment coverage across 119 lines.

**File:** `modules/ccxt_exchanges.js`

**Fix:** Add JSDoc/docstrings to exported functions, classes, and complex logic.

---

#### 🔵 Insufficient Documentation

**Severity:** `LOW`

modules/gru_engine.js has < 5% comment coverage across 102 lines.

**File:** `modules/gru_engine.js`

**Fix:** Add JSDoc/docstrings to exported functions, classes, and complex logic.

---

#### 🔵 Insufficient Documentation

**Severity:** `LOW`

public/translations.js has < 5% comment coverage across 532 lines.

**File:** `public/translations.js`

**Fix:** Add JSDoc/docstrings to exported functions, classes, and complex logic.

---

#### 🔵 Insufficient Documentation

**Severity:** `LOW`

scripts/backtest_vision_full.py has < 5% comment coverage across 320 lines.

**File:** `scripts/backtest_vision_full.py`

**Fix:** Add JSDoc/docstrings to exported functions, classes, and complex logic.

---


---

*Generated by [claude-audit](https://github.com/itsmesherry/claude-audit) — AI-powered codebase auditor*