# AUDIT NEXUS V9 — 18.05.2026 07:56

**Auditor**: Senior-Audit-Engineer (Claude Opus 4.7, READ-ONLY Mode)
**Bot-Status zur Audit-Zeit**: PM2 R=101, Wallet 999.02 USDT, DEPLOY_MODE=PAPER, Drift=0, AladdinBrain ~1666/h
**Methodik**: Code-Inspektion server.js (25717 LOC) + modules/ (7 Dateien) + public/index.html (8934 LOC) + nexus.db (444 MB) + .env + ecosystem.config.js. Live-Endpoints abgefragt. Keine Änderungen.

---

## EXECUTIVE SUMMARY — TOP-5-RISIKEN

| # | ID | Severity | Beschreibung |
|---|----|----------|--------------|
| 1 | AUD-001 | **KRITISCH** | `/api/deploy`, `/api/mode/live`, `/api/kill` haben KEINE Auth-Middleware — jeder mit Bot-Port-Zugriff kann LIVE schalten oder Bot killen |
| 2 | AUD-002 | **HOCH** | `getEffectiveDemoEquity()` zählt mbtCommitted doppelt → effectiveTotal ~1239 statt ehrlicher ~1014. Fix-Versuch heute als X gerollt zurück weil peakTotal-Stale Side-Effect (KillSwitch HARD KILL). Braucht koordinierten X+peakTotal-Reset |
| 3 | AUD-003 | **HOCH** | Order-Send fehlt `clientOrderId` (Idempotenz). Bei Network-Retry möglich Doppel-Order in LIVE-Mode |
| 4 | AUD-004 | **HOCH** | Reserve wächst NUR aus SINGLE-Trade-Wins (Balance.recordProfit). Grid `profit_acc` (18.94 USDT) und DCA-TP-Hits gehen NICHT durch applyPnL → bei MBT-dominiertem Trading bleibt Reserve permanent klein |
| 5 | AUD-005 | **MITTEL** | SCORE_FLOOR-Konfiguration ambivalent: CFG.SCORE_FLOOR=0.08, SCORE_FLOOR_OLD=0.04, SCORE_FLOOR_MODE='log_only' → Direction nutzt weiterhin 0.04, 0.08 wird nur geloggt. Doku im Code sagt klar, Verhalten muss Christian explizit kennen |

---

## SEVERITY-VERTEILUNG

| Stufe | Anzahl |
|-------|-------:|
| KRITISCH | 1 |
| HOCH | 3 |
| MITTEL | 7 |
| NIEDRIG | 5 |
| INFO | 4 |
| **TOTAL** | **20** |

---

## 1. INVENTAR

### Workspace
- Root: `/Users/christianheilig/NEXUS_CLEAN`
- `server.js`: 25717 LOC, 1.23 MB, mtime 18.05.2026 07:47 (FIX-W heute)
- `public/index.html`: 8934 LOC, 553 KB, mtime 16.05.2026 22:00
- `nexus.db`: ~444 MB (WAL-Mode), 242k+ Zeilen logisch
- `.env`: 7 Zeilen, chmod 600 ✅, alle 7 Secret-Felder gesetzt
- `.env.example`: vorhanden ✅
- `.gitignore`: vorhanden, schützt `.env`, `nexus.db`, `node_modules`, `*.bak.*` ✅
- `ecosystem.config.js` (PM2, NICHT .json wie im Briefing): "nexus" + Sub-Bot-Slot "nexus_sub_1" (autostart=false)
- **212 .bak-Dateien** im Workspace (sehr viele Backup-Historie)

### modules/ Verzeichnis (TIER2 Pipeline 17.05.)
| Datei | LOC | Status |
|-------|-----|--------|
| walkforward.js | 196 | production-ready |
| stresstest.js | 195 | production-ready |
| perfattrib.js | 196 | production-ready |
| hyperopt.js | 187 | Scaffold (Random+HillClimb statt Bayes) |
| lstm_v5.js | 235 | Scaffold (LogReg-Surrogate statt LSTM) |
| ccxt_exchanges.js | 118 | production-ready (5 Exchanges, read-only) |
| freqai_features.js | 334 | production-ready (99 Features) |

### Top-Level Module in server.js (40+)
CFG, OrderBatch, Log, ExchangeRegistry, ScriptEngine, Bitget, Ind, FundingEngine, Safeties, FlashCrashBot, HistoricalData, TickBacktest, MLOptimizer, PerfTracker, TrainingBridge, Balance, KillSwitch, Incidents, Regime, Strategies, RiskLadder, NoTrade, PositionGuardian, ExitEngine, Trades, StatsCore, RiskSizing, RegimeStrength, StressTest, Recon, ConsistencyGuardian, MLModelArchiver, EventLoopHealthMonitor, LiveTier, DBSplit, SessionFilter, MetricsExport, ProfitLockHWM, StrategyRotation, LSTMShadow, MetaBrain, AladdinBrain, DemoEngine, WalletProvider, ComboBot, SentimentScaler, CapitalPool, StrategySequence, MBTTicker, DailyLossEmergencyStop, GridBotMBT, InfinityGridBotMBT, DCABotMBT.

---

## 2. ARCHITEKTUR-MAP

### Datenfluss (verifiziert via Code-Lese)
```
Bitget REST/WS (price+orderbook+ticker)
  ↓
UnifiedScore.compute (21 Quellen, inkl. FearGreed + News + ML)
  ↓
AladdinBrain.decide (5-Family-Consensus + 7 Hard-Blocks)
  ↓ (vetos resp.)
MetaBrain.decide → metaRegime + primary-Strategy + botType
  ↓
StrategySequence.evaluate (Superalgos-Pattern, 7 Strategien, Regime-Gate, Cooldown)
  ↓
CapitalPool.canAllocate (40/25/20/15 + maxActive)
  ↓ DemoEngine._handleMultiBotType (GRID/DCA/INFGRID)
  ODER _cycle → ExecutionAdapter.placeOrder
  ↓
ExecutionAdapter:
  DEMO → _simulateFill (orderbook walk + slippage + latency)
  LIVE → _liveFill (Bitget.placeSportOrder, gated by CFG.API_KEY + DEPLOY_MODE)
  ↓
Trades.close() → applyPnL → Balance/DemoEngine.wallet → wallet_ledger
```

### MBTTicker (parallel zum Cycle)
- 30s Independent-Tick (Z.9217)
- Ticked DCA.scheduledBuy + Grid.onPriceUpdate
- Boot-Delay 60s, busy-Lock vorhanden

### Tote/Dekorative Module
- **PlaceBestExecutionOrder** (Z.7845): hardcoded `NOT_IMPLEMENTED_NEEDS_CCXT` Stub. Drei Schutz-Schranken, aber Real-Send-Pfad fehlt. SCHEINLOGIK markiert.
- Wyckoff: 0 Vorkommen im Code (in Vision dokumentiert, aber nie gebaut). SCHEINLOGIK.
- Multi-TF Indikatoren: 0 Vorkommen. SCHEINLOGIK.
- CMO-Indikator: 0 Vorkommen (Vision mentioned). SCHEINLOGIK.

---

## 3. SYNTAX + STATISCHE FEHLER

**server.js + alle 7 modules: `node --check` ✅ alle OK.**

### Beobachtungen
- 472 Express-Routen — sehr breite API-Fläche
- 557 `.toFixed()` Calls — gut für Float-Display, aber Money-Arithmetik selbst nutzt nur Number (kein BigDecimal) → potenzielle Drift bei langen Laufzeiten (siehe AUD-013)
- 546 Date.now()/new Date()/setTimeout-Vorkommen — viele Zeitabhängigkeiten, kein zentralisiertes ClockProvider
- `TODO` / `FIXME` / `XXX` Tags: **0 im Code** (positiv — keine offenen Markierungen)
- 38 Top-Level-Module — eng gekoppelt, gemeinsame State-Variables (DemoEngine.wallet, Balance.*, KillSwitch.mode) global zugreifbar

---

## 4. TRADING-LOGIK (NEXUS-SPEZIFISCH)

### SCORE_FLOOR — AKTUELLER STAND (Code Z.170-182, Z.11154-11157)
```js
SCORE_FLOOR:           0.08,
SCORE_FLOOR_OLD:       0.04,
SCORE_FLOOR_MODE:      'log_only',  // 'log_only' | 'active_block'
```

**🟡 AUD-005 (MITTEL)**: SCORE_FLOOR-Hybrid-Modus
- `SCORE_FLOOR_MODE='log_only'` → Direction-Logik nutzt weiterhin 0.04 (alter Wert), 0.08 wird nur geloggt als "would-block"
- Code-Kommentar bestätigt: "Wirksam erst wenn SCORE_FLOOR_MODE='active_block' UND AUTONOMOUS_DEMO_TRADES=true"
- AUTONOMOUS_DEMO_TRADES_ENABLED ist `true` in DB
- Aber SCORE_FLOOR_MODE bleibt log_only → 0.08 NICHT aktiv enforced
- **Auswirkung**: Bot tradet weiterhin mit Schwelle 0.04, 0.08-Beobachtung läuft "schattenhaft"
- **Empfehlung**: dokumentieren ob das gewollt ist; sonst auf 'active_block' ziehen

### Bayesian Confidence (Z.12330)
```js
const confidence = Math.min(0.75, posteriors[dominant]);
```
✅ **Cap auf 0.75 vorhanden** (entspricht Briefing-Vorgabe)
✅ `learnPriors=false` Default mit Stabilized Forgetting (Z.12276-12308) — Priors lernen NUR bei echten Trade-Outcomes (richtig konservativ)

### Bayesian-Symmetrie
- BAYESIAN_BEAR + BAYESIAN_BULL beide als Hard-Block (Z.11167, Z.11171) ✅ symmetrisch implementiert
- Letzte 24h: BAYESIAN_BEAR 853 Vetos, BAYESIAN_BULL 0 → Markt aktuell ranging/leicht-bearish

### EXTREME_VOL (Z.11057 Kommentar + Z.8160 Klassifikation)
```js
/* Patch 27.04.2026: EXTREME_VOL Hard-Block entfernt - positionScale 0.25 bleibt aktiv */
```
- Hard-Block bei EXTREME_VOL ist **ENTFERNT**
- Stattdessen: `positionScale 0.25` wirkt (Sizing-Dämpfung statt Block)
- Klassifikation als 'EXTREME_VOL' bei `vol>0.15 || volRegime==='EXTREME'` (Z.8160) bleibt
- In MetaBrain-Mapping: EXTREME_VOL → CONSERVATIVE-Strategy (kein Trade)
- ✅ Konsistent: Block durch CONSERVATIVE-Strategy-Routing, nicht durch Hard-Veto

### BRAIN_MODE (Z.91, Z.11325-11334)
```js
BRAIN_MODE: "voter"  // shadow → voter (Brain entscheidet mit, nicht nur beobachtet)
```
- Mode in [shadow, voter, authority]
- Aktuell: **voter** (Brain hat Stimme bei Decisions)
- Switch via POST `/api/cfg/set` mit Key=BRAIN_MODE — siehe Z.13256-13259, schreibt in bot_settings DB-Persistenz
- **🟡 AUD-006 (MITTEL)**: Switch ist 1-Zeilen-DB-INSERT ohne Auth + ohne explizite Bestätigung. Authority würde Brain zum Final-Decider machen.

### AUTONOMOUS_DEMO_TRADES_ENABLED (Z.96)
```js
AUTONOMOUS_DEMO_TRADES_ENABLED: false  // Code default
```
- **DB Override: `true`** in `bot_settings` ✅
- DemoEngine liest DB-Wert, der Code-Default ist false (sicher)
- Konsistent: Bot tradet autonom in DEMO

### Position-Sizing (Z.4651-4653)
```js
calcPositionSize(kelly=0.5) {
  const base = kelly * this.trading * CFG.KELLY_FRACTION;  // = 0.05
  return Math.max(0, Math.min(base, this.trading * CFG.MAX_POSITION_PCT));  // = 0.10 cap
}
```
- KELLY_FRACTION=0.05 ✅ (Briefing)
- MAX_POSITION_PCT=0.10 → max 10% des Trading-Capitals pro Trade
- Min-Notional MIN_POSITION_USDT=5 (CFG)
- **🟡 AUD-007 (MITTEL)**: KEINE Tick-/Lot-Size-Validierung gegen Bitget-Symbol-Specs. Order kann von Exchange rejected werden wenn Size nicht zum Lot-Size passt. Kein Pre-Send-Adjustment.

### Hebel/Leverage (Z.238-239, 1835-1860)
- DEFAULT_LEVERAGE=3, MAX_LEVERAGE=10
- `Bitget.setLeverage()` Z.1835 wird VOR jeder `placeFuturesOrder` aufgerufen (Z.1851)
- **🟢 AUD-008 (NIEDRIG)**: Leverage-Set ist nicht idempotent — bei Network-Retry könnte mehrfach gesetzt werden. Aber: Bitget-API ist idempotent für gleichen Leverage-Wert ✅

### Order-Lifecycle: SL/TP/Trailing (Z.5331-5400)
- ExitEngine.tpslLevels mit `{stopLoss, takeProfit, trailHigh, atr, side}` ✅
- Adaptive SL/TP via ProfitOptimizer (Z.5340)
- Trailing-Stop via ProfitLock Z.5187-5202 ✅
- Partial-Fill-Handling Z.10379 + Z.11883 (Incident-System) ✅
- Reject-Handling: Bitget order.code !== '00000' → return error ✅

### Fees + Slippage in PnL (Z.5474-5588)
```js
const entryFee = trade.size * CFG.TAKER_FEE;       // 0.0006
const exitFee  = (exitPrice * coinAmount) * CFG.TAKER_FEE;
// gross - fees = pnl
```
- TAKER_FEE=0.0006 (Bitget Spot)
- MAKER_FEE=0.0002
- MAX_SLIPPAGE_PCT=0.005 (0.5% pre-flight check)
- ✅ Fees beim Close korrekt abgezogen
- **🔵 AUD-009 (NIEDRIG)**: Funding-Fees für Futures werden in FundingEngine berechnet (Z.2825) aber nicht klar in Trade-PnL eingerechnet. Bei Futures-Hold > 8h kann das spürbar werden.

---

## 5. 70/30-SPLIT & RECONCILIATION

### Code (Z.124-125, 4656-4670, 9970-10001)
```js
RESERVE_RATIO: 0.70,
TRADING_RATIO: 0.30,
recordProfit(profit) {
  const rs = profit * 0.70;
  const ts = profit * 0.30;
  this.reserve += rs;
  this.trading += ts;
}
```

### 🔴 AUD-004 (HOCH): Reserve-Routing-Lücke für MBT-Profits
- `Balance.recordProfit()` (LIVE-Pfad) wird in `Trades.close()` Z.5484 aufgerufen → Reserve wächst korrekt
- `DemoEngine.applyPnL()` Z.9970 macht ebenfalls 70/30 → korrekt
- ABER: GridBotMBT und DCABotMBT haben EIGENE Profit-Logik:
  - GRID_SELL-Fills schreiben in `wallet_ledger` (Z.8756) und `grid_instances.profit_acc` (Z.8734)
  - Aber gehen NICHT durch `DemoEngine.applyPnL()` → kein 70/30-Split
- **DB-Beleg**: PROFIT_SPLIT_RESERVE: nur 10 Ops, letzter 16.05. — kein PROFIT_SPLIT seit MBT dominierte Aktivität
- Aktuelle Grid-Profits: DOGE 10.69 + ATOM 7.50 + INFGRID 0.25 = **18.94 USDT realized**, davon 0 in Reserve
- Heutiger Tag: 0 SINGLE-Closures, alle Aktivität ist MBT → Reserve stagniert bei 15.05
- **Auswirkung**: Reserve-Spec ist gebrochen — 70/30 wirkt nur noch für legacy SINGLE-Trades
- **Empfehlung**: `routeMBTProfitToReserve()` Helper-Spec liegt bereits in `~/Desktop/NEXUS_BACKUPS/KAPITAL_ATOM_FIX_20260518_074057/Y_reserve/Y_DEFERRED_helper_spec.md`

### Reconciliation (Z.5988-6065, 12711, 24189)
- `Recon.run()` läuft alle 5min via setInterval (Z.24189)
- Endpoint `/api/recon/check` aktuell: `drift=0, consistent=true` ✅
- Soll-Ist-Check: `sollTrading=983.98` vs `istTrading=983.98` ✅
- ConsistencyGuardian (Z.6066+) prüft WALLET_DRIFT, WALLET_NEG, DB_ERROR, ERROR_SPIKE ✅

### 🟠 AUD-002 (HOCH): getEffectiveDemoEquity() Doppel-Count
- Code Z.10125: `effectiveTotal: total + mbtCommitted + unrealized.total`
- `total` enthält bereits implizit mbtCommitted (DCA_BUY zieht KEIN Cash vom Wallet-Total ab — `before_total==after_total` in jedem Ledger-Eintrag)
- → effectiveTotal zeigt ~1239 statt ehrlicher ~1014
- Fix-Versuch heute Morgen als X gerollt zurück: korrigierter effectiveTotal=1014 vs alter peakTotal=1217.57 (DemoEngine.wallet.peakTotal) → DD-Berechnung in `KillSwitch.check()` ergab 16.67% > 12% → HARD KILL
- **Empfehlung**: Koordinierte Pipeline mit (a) Code-Fix in Z.10125 UND (b) Einmal-Daten-Migration `DemoEngine.wallet.peakTotal = aktueller ehrlicher effectiveTotal` UND (c) demo_wallet.json schreiben.

### Audit-Trail
- `wallet_ledger` Tabelle: 11+ Op-Typen, jeder Trade dokumentiert (before/after)
- `consistency_log` + `consistency_log_archive` Tabellen ✅
- `system_log` + `system_log_archive` Tabellen ✅
- Mode-Switches: über `bot_settings.updated_at` nachverfolgbar
- **🔵 AUD-010 (NIEDRIG)**: Keine dedizierte `mode_switch_audit` Tabelle — Mode-Switches landen nur in PM2-Logs

---

## 6. ENDPOINTS & API

### Express-Routen
- **472 Routen total**
- 6 sensible Routen identifiziert:
  - `/api/deploy` (Mode-Switch) — Z.14603
  - `/api/mode/live`, `/api/mode/demo`, `/api/mode/switch`, `/api/mode/toggle` — Z.15968-15988
  - `/api/kill`, `/api/kill/reset` — Z.13449
  - `/api/risktier/set`, `/api/risktier/dryrun/enable` — Z.17077-17079
  - `/api/cfg/set` (kann BRAIN_MODE switchen) — Z.13256
  - `/api/ml/rollback`, `/api/ml/persist/reset` — Z.14363, 14403

### Bitget REST
- `Bitget.placeSportOrder` (Z.1819) + `placeFuturesOrder` (Z.1843) + `setLeverage` (Z.1835) + `cancelOrder` (Z.1827)
- Auth: HMAC-SHA256 Signatur ✅
- **🟠 AUD-003 (HOCH)**: KEINE clientOrderId/Idempotency-Key bei Order-Send. Bei Network-Retry (ECONNRESET-Recovery Z.10810) könnte same Order zweimal landen.

### Bitget WebSocket
- Auto-Reconnect mit exponential backoff (Z.2006-2010): 3s × 2^attempt, max 30s ✅
- Heartbeat/Pong-based Watchdog: Force-Reconnect bei 8min, Process-Exit bei 15min ✅
- Subscription-Recovery beim Reconnect: vorhanden ✅
- Fallback auf DB-Cache: Bitget.priceCache ✅

### Interne Express-Routen
- **🔴 AUD-001 (KRITISCH)**: KEINE Auth-Middleware für ANY Endpoint
  - grep für `passport|jwt|express-session|requireAuth|adminOnly|authMiddleware`: **0 matches**
  - Folge: jede Person mit Port-3000-Zugriff kann:
    - POST /api/deploy {mode:'LIVE_FULL'} → Bot in LIVE schalten
    - POST /api/mode/live → DemoEngine.switchToLive()
    - POST /api/kill → KillSwitch HARD KILL → Bot stoppt
  - Aktueller Schutz: Bot lauscht nur auf localhost (kein 0.0.0.0 expliziert)? Muss geprüft werden
  - **Empfehlung**: Mindestens IP-Whitelist + Token-Header für sensitive Routes (NICHT umsetzen jetzt, nur vorschlagen)

### Rate-Limit
- Express ratelimit middleware Z.56: 429 bei Too Many Requests ✅
- Bitget-Rate-Limit-Probe vorhanden (Z.7619-7631) ✅

---

## 7. SECURITY

### Secrets
- ✅ KEINE hardcoded API-Keys/Tokens im Code (grep -E "(api_key|password|token|secret)\s*=\s*['\"][a-zA-Z0-9]{20,}" → 0 matches)
- ✅ .env via dotenv geladen (Z.7)
- ✅ .env in .gitignore (verified)
- ✅ .env chmod 600 (owner only)
- ✅ ecosystem.config.js inherit'ed Secrets aus Shell-Env (kein hardcode)

### Logging-Hygiene
- ✅ Keine offensichtlichen Secret-Leaks in `Log.*` Calls
- 🔵 **AUD-011 (NIEDRIG)**: Stack-Traces bei catch werden via Log.warn ausgegeben — könnte bei Bitget-Error API-Sig im URL leaken. Quick-Check: bei Bitget-Auth-Errors `err.message` wird ohne Filter geloggt.

### Auth/Session — schon im KRITISCH AUD-001 behandelt

### Fail-Secure
- ✅ KillSwitch.HALTED state stoppt aktiv Trading (alle AladdinBrain-Decisions vetoed mit KILL_SWITCH_ACTIVE)
- ✅ DailyLoss bei realized < -15 USDT → autonomous_demo_trades_enabled='false' in DB
- ✅ DEPLOY_MODE default 'PAPER' wenn nicht in env

### Audit-Trail
- `consistency_log` + `system_log` Tabellen ✅
- `wallet_ledger` für alle Money-Flows ✅
- `aladdin_decisions` für jeden Brain-Output ✅
- **🟡 AUD-012 (MITTEL)**: Keine separate `audit_trail`-Tabelle für sicherheitsrelevante Aktionen (Mode-Switch, Risk-Change, Manual-Order). Diese landen nur in PM2-Logs, die rotated und überschrieben werden.

---

## 8. FRONTEND/UI

### Tabs/Routen
- Dashboard `public/index.html` (8934 LOC)
- Tab-Struktur per `data-tab=` Attribut (grep zeigte 0 — entweder andere Mechanik wie JS-Switch)
- **🔵 AUD-014 (NIEDRIG)**: Tab-Mechanismus nicht via data-tab, sondern wahrscheinlich via JS-Class-Toggle — schlechter für Bookmarking/Deep-Links

### Confirm-Dialogs vor kritischen Aktionen ✅
10 confirm()-Vorkommen identifiziert:
- Z.538: "ALLE BOTS STOPPEN?" → /api/botmanager/emergency
- Z.3598: "DCA schließen?" / "Force-Buy?"
- Z.3612: "${botType} schließen?"
- Z.4893: "Wallet zurücksetzen auf X USDT?" (mit Hinweis "Im LIVE-Modus passiert NICHTS")
- Z.5022: "NOT-AUS aktivieren?" → /api/kill
- Z.5064: API-Keys löschen
- Z.6021: "Alle Bots für X stoppen?"

Plus Modal-Dialog für Trade-Bestätigung (Z.3065)

### "[object Object]" Anti-Pattern
- ✅ 0 Vorkommen im HTML

### Punk-Style-Klassen
- 6 Vorkommen von `.ct`, `black-translucent`, `mono.xs`, `line-height 2.4` zusammen
- Wahrscheinlich konsistent angewendet (würde Detail-Audit brauchen)

### Mobile Viewport
```html
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
```
✅ vorhanden, aber **🔵 AUD-015 (NIEDRIG)**: `user-scalable=no` ist Accessibility-Anti-Pattern (WCAG-Verstoß) — User können auf iPhone nicht zoomen

### Re-Auth vor LIVE
- **🟠 AUD-016 (MITTEL)**: confirm()-Dialoge sind reine Click-Bestätigungen, KEIN Re-Auth (kein PIN/Password/2FA)
- Z.3065 Modal "TRADE BESTÄTIGEN" hat nur "BESTÄTIGEN"-Button, kein PIN-Field

---

## 9. KRITISCHE-AKTION-GATES

| Aktion | Gate vorhanden? | Re-Auth? | Audit-Eintrag? |
|--------|-----------------|----------|----------------|
| PAPER→LIVE Switch (`/api/deploy`) | Modus-Validierung ✅ | ❌ | Nur PM2-Log |
| `/api/mode/live` (DemoEngine.switchToLive) | API_KEY-Check + openPositions-Check + StatsCore.liveReady + force-Override | ❌ | Telegram-Alert ✅ |
| `/api/kill` Manual-Kill | Confirm-Dialog (UI) | ❌ | KillSwitch._hardKill mit MANUAL-reason ✅ |
| `/api/cfg/set` (BRAIN_MODE Switch) | Whitelist `[shadow,voter,authority]` | ❌ | DB-Persistenz + Log.warn ✅ |
| `/api/risktier/set` | RiskTier.setTier-Whitelist | ❌ | Log ✅ |
| `/api/ml/rollback` | nicht geprüft | ❌ | nicht geprüft |
| `/api/wallets/:addr` DELETE | DELETE-Methode | ❌ | nicht geprüft |

**🔴 AUD-001 (KRITISCH, recurring)**: Kein Endpoint hat Re-Auth-Layer. switchToLive() hat zwar mehrere Pre-Gates (API_KEY + openPositions + StatsCore-LiveReady) aber alle mit `force=true` umgehbar.

---

## 10. TESTS & LOGGING

### Tests
- **❌ KEIN `tests/` oder `test/` Verzeichnis**
- 10+ `.tier*_test.js` / `.welle*_test.js` als Smoke-Test-Skripte (versteckte Dateien)
- 0 unit-test framework (kein jest/mocha/vitest in dependencies)
- **🟠 AUD-017 (MITTEL)**: KEINE Test-Coverage gegen Trading-Logik. Refactoring ist riskant.

### Logging
- `Log.info`/`warn`/`error`/`boot` mit kategorisiertem Namespace
- PM2 logs mit timestamp format `YYYY-MM-DD HH:mm:ss.SSS` ✅
- pm2-logrotate installiert (Modul-Liste) ✅
- Log-Level: kein env-konfigurierbares LOG_LEVEL gefunden (alles always-on)
- **🔵 AUD-018 (NIEDRIG)**: Bei Verbose-Trading-Aktivität (1666 AladdinBrain/h) ist Log-Volume hoch — pm2-logrotate sollte konfiguriert sein (max_size + retain)

---

## 11. SCHEINLOGIK / DEFERRED FEATURES

| # | Feature | Status | Beleg |
|---|---------|--------|-------|
| 11.1 | **Wyckoff** | Vision dokumentiert, **0 Code-Vorkommen** | grep ergebnislos |
| 11.2 | **Multi-TF (Multi-Timeframe)** | 0 Vorkommen | grep ergebnislos |
| 11.3 | **CMO-Indikator** | 0 Vorkommen | Vision mentioned |
| 11.4 | **PlaceBestExecutionOrder** (Z.7845-7886) | Stub mit `NOT_IMPLEMENTED_NEEDS_CCXT` | Code-Kommentar bestätigt |
| 11.5 | **Walk-Forward-Optimization** | TIER2-A vom 17.05. ✅ real implementiert | modules/walkforward.js (196 LOC) |
| 11.6 | **LSTM v5** | Logistic-Regression-Surrogate statt echter LSTM | modules/lstm_v5.js (dir_acc 48.5%) |
| 11.7 | **Hyperopt** | Pure-JS Random+HillClimb, NICHT Bayesian-Opt | modules/hyperopt.js (kein Optuna) |
| 11.8 | **Multi-Exchange Order-Routing** | Read-only via CCXT, Order-Send blockiert | Schranken 1-3 in PlaceBestExecutionOrder |
| 11.9 | **SuperAlgos-Inspirationen** | StrategySequence umgesetzt ✅ | Z.8334+ funktional |
| 11.10 | **Visual Debugger** | nicht im Code gefunden | grep ergebnislos |
| 11.11 | **Newsfeed-Slot** | RSSAggregator + news_feed-Tabelle ✅ funktional | Z.12623+ |
| 11.12 | **Reset Day Zero** | Spec vorhanden, kein resetDayZero()-Code | grep ergebnislos |

**Empfehlung Scheinlogik-Pflege**: Vision-Dokumente und Code-Reality klar trennen — mehrere Vision-Features (Wyckoff, MultiTF, CMO, Reset Day Zero) haben 0 Code, sind aber in CLAUDE.md/Roadmap-Texten als "geplant" oder gar "deployed" markiert. Risiko: false expectations.

---

## 12. EDGE CASES

### WebSocket Disconnect → Reconnect
- ✅ Exponential backoff 3s × 2^n, max 30s
- ✅ Pong-based Watchdog (8min force-reconnect, 15min process-exit)
- ✅ Subscription-Recovery

### Bitget Rate-Limit (429)
- ✅ Express ratelimit middleware
- ✅ Rate-Limit-Probe in Pre-Live-Check (Z.7619)
- ✅ Type-mapped Healing (Z.10811-10812): ECONNRESET → API_RETRY

### Crash-Recovery aus SQLite
- ✅ DemoEngine.wallet via demo_wallet.json (Persistenz)
- ✅ Positions via demo_positions.json
- ✅ Balance.restoreReserveFromDB() (Z.4678)
- ✅ DCA/Grid-Instanzen in DB (überleben Restart)

### Doppelstart via PM2
- ✅ PM2 sorgt für single-instance pro App-Name
- 🔵 AUD-019 (NIEDRIG): Kein expliziter pidfile-Lock im Code als Defense-in-Depth

### Uhr-Drift / Zeitzonen
- 🟡 **AUD-013 (MITTEL)**: 546 Date.now/setTimeout-Vorkommen, kein zentralisierter ClockProvider
- DailyLoss._triggeredDay nutzt `new Date().toDateString()` → Localtime → bei TZ-Wechsel (Sommer/Winter) könnte Tageswechsel inkonsistent sein
- 🔵 AUD-020 (NIEDRIG): Float-Drift bei langen Laufzeiten — Number.EPSILON für Money. Aktuell `total - reserve - trading = 7.4e-14` (vernachlässigbar)

### Doppel-Trigger Cycle
- ✅ `_cycleBusy` Lock (Z.21796-21798) verhindert konkurrierende Cycle-Aufrufe
- ✅ MBTTicker `busy` Lock (Z.9231)

---

## SCORE-SCHWELLEN-DISKREPANZ (Briefing-Anfrage)

**Briefing-Kontext**: "SCORE_FLOOR historisch zwischen 0.04 und 0.08 hin- und hergewandert"

**Heutiger Code-Stand**:
```js
CFG.SCORE_FLOOR     = 0.08      // neue strenge Schwelle
CFG.SCORE_FLOOR_OLD = 0.04      // alter Wert
CFG.SCORE_FLOOR_MODE = 'log_only' // 'active_block' wäre Vollumsetzung
```

**Effektives Verhalten**:
- Direction-Logik nutzt **weiterhin 0.04** (Z.11154-11157 lädt beide, aber MODE=log_only)
- 0.08-Schwelle wird nur als "would-block" geloggt
- ✅ A/B-Modus aktiv (klar im Code dokumentiert Z.171-178)
- ❌ Aber: in CLAUDE.md V14.6 wurde "Welle 2a" als "live deployed" markiert ohne klarzustellen dass MODE=log_only ist

**Empfehlung**: explizit dokumentieren ob SCORE_FLOOR_MODE='log_only' Dauer-Zustand oder Übergangs-Modus ist. Wenn dauerhaft → wert auf 0.04 ziehen, MODE-Variable entfernen.

---

## VOLLSTÄNDIGE BEFUND-LISTE

### AUD-001 — KRITISCH — Security
- **Datei:Zeile**: server.js (global)
- **Kategorie**: Security / Endpoint
- **Beschreibung**: Keine Auth-Middleware für sensible Endpoints (`/api/deploy`, `/api/mode/live`, `/api/kill`, `/api/cfg/set`, `/api/risktier/set`, ML-Endpoints)
- **Beleg**: `grep -E "passport|jwt|express-session|requireAuth"` → 0 matches
- **Auswirkung worst case**: Jemand mit Netzwerk-Zugriff auf Port 3000 kann Bot in LIVE schalten, killen, oder Risk auf Maximum stellen
- **Empfehlung**: IP-Whitelist + Static-Token-Header für `/api/deploy|mode|kill|cfg|risktier|ml/*`. Auch Tailscale-only-bind als Defense-in-Depth.
- **Freigabe**: ja
- **Unsicherheit**: nein

### AUD-002 — HOCH — Rechnung
- **Datei:Zeile**: server.js:10125
- **Kategorie**: Rechnung / Reconciliation
- **Beschreibung**: `getEffectiveDemoEquity()` zählt `mbtCommitted` doppelt (steckt schon in `total`)
- **Beleg**: `effectiveTotal: Number((total + mbtCommitted + unrealized.total).toFixed(4))` — Wallet-Ledger zeigt `before_total==after_total` bei DCA_BUY
- **Auswirkung**: UI/KillSwitch/Heartbeat zeigen falsche "Vermögen" ~1239 statt ehrlicher ~1014
- **Empfehlung**: Code: `effectiveTotal: total + unrealized.total` + Daten-Migration `DemoEngine.wallet.peakTotal` Reset
- **Freigabe**: ja (Wallet-Touch nötig)
- **Unsicherheit**: nein

### AUD-003 — HOCH — Idempotenz
- **Datei:Zeile**: server.js:1819 (placeSportOrder)
- **Kategorie**: Async / Trading-Logik
- **Beschreibung**: Order-Send ohne `clientOrderId` (kein Idempotency-Key)
- **Beleg**: `grep -E "clientOrderId|idempotency"` → 0 matches
- **Auswirkung**: Bei Network-Retry kann gleiche Order doppelt landen → echtes Geld doppelt eingesetzt in LIVE
- **Empfehlung**: UUID generieren pro Order + als `clientOrderId` an Bitget mitschicken; Order-Cache prüfen vor Retry
- **Freigabe**: ja
- **Unsicherheit**: nein

### AUD-004 — HOCH — 70/30
- **Datei:Zeile**: server.js:9970 (DemoEngine.applyPnL) vs server.js:8756 (GridBot ledger)
- **Kategorie**: 70/30 / Reconciliation
- **Beschreibung**: Grid/DCA-Profits gehen NICHT durch `applyPnL` → Reserve wächst nicht aus MBT
- **Beleg**: 10 PROFIT_SPLIT_RESERVE-Ops lifetime, letzter 16.05. trotz aktiver Grid-Profits 18.94 USDT
- **Auswirkung**: Bei MBT-dominiertem Trading bleibt Reserve permanent klein → 70/30-Spec gebrochen
- **Empfehlung**: `routeMBTProfitToReserve()` Helper (siehe `~/Desktop/NEXUS_BACKUPS/KAPITAL_ATOM_FIX_20260518_074057/Y_reserve/Y_DEFERRED_helper_spec.md`) — Aufrufe in Grid-SELL-Fill + DCA-TP-Hit
- **Freigabe**: ja (Trading-Logik-Touch)
- **Unsicherheit**: nein

### AUD-005 — MITTEL — Trading-Logik
- **Datei:Zeile**: server.js:170-182, 11154-11157
- **Kategorie**: Trading-Logik
- **Beschreibung**: SCORE_FLOOR hybrid mode — 0.04 effektiv aktiv, 0.08 nur log_only
- **Beleg**: `SCORE_FLOOR_MODE: 'log_only'`
- **Auswirkung**: Schwelle ist softer als Dokumentation impliziert
- **Empfehlung**: Entscheiden ob log_only Übergang oder Dauer → Code aufräumen
- **Freigabe**: ja
- **Unsicherheit**: nein

### AUD-006 — MITTEL — Security
- **Datei:Zeile**: server.js:13256
- **Kategorie**: Security / Trading-Logik
- **Beschreibung**: BRAIN_MODE-Switch via `/api/cfg/set` ohne Auth + Audit
- **Beleg**: nur Log.warn, kein dedizierter Audit-Eintrag
- **Auswirkung**: Brain könnte auf `authority` ohne Spur gesetzt werden
- **Empfehlung**: Re-Auth + dedicated `audit_trail`-Insert
- **Freigabe**: ja
- **Unsicherheit**: nein

### AUD-007 — MITTEL — Trading-Logik
- **Datei:Zeile**: server.js:4651
- **Kategorie**: Trading-Logik / Endpoint
- **Beschreibung**: Keine Tick-/Lot-Size-Validierung vor Order-Send
- **Beleg**: `calcPositionSize` returnt USDT-Wert, ohne Bitget-Spec-Adjustment
- **Auswirkung**: Order kann von Exchange rejected werden mit "size below min" oder "size not multiple of lot"
- **Empfehlung**: Bitget.getSymbolInfo() cachen, in placeSportOrder size auf lotSize runden
- **Freigabe**: ja
- **Unsicherheit**: nein

### AUD-008 — NIEDRIG — Trading-Logik
- **Datei:Zeile**: server.js:1851
- **Kategorie**: Trading-Logik
- **Beschreibung**: Leverage-Set vor jedem Futures-Order — nicht idempotent in code, aber API-seitig OK
- **Auswirkung**: Minimal — extra-API-Call
- **Empfehlung**: leverage-cache nutzen
- **Freigabe**: nein
- **Unsicherheit**: nein

### AUD-009 — NIEDRIG — Rechnung
- **Datei:Zeile**: server.js:2825 (FundingEngine)
- **Kategorie**: Rechnung
- **Beschreibung**: Funding-Fees nicht klar in Futures-Trade-PnL eingerechnet
- **Auswirkung**: Bei Hold > 8h kann PnL signifikant abweichen
- **Empfehlung**: FundingEngine-Output in Trades.close() addieren
- **Freigabe**: ja
- **Unsicherheit**: ja (FundingEngine-Code nicht detailliert geprüft)

### AUD-010 — NIEDRIG — Audit-Trail
- **Datei:Zeile**: server.js (global)
- **Beschreibung**: Keine separate `audit_trail`-Tabelle für Mode-Switches/Risk-Changes
- **Empfehlung**: dedizierte Tabelle mit immutable rows
- **Freigabe**: ja

### AUD-011 — NIEDRIG — Security
- **Datei:Zeile**: Bitget-Auth-Errors
- **Beschreibung**: Stack-Traces könnten URL-Signatures leaken
- **Empfehlung**: Log-Filter für Sig-URLs
- **Freigabe**: nein
- **Unsicherheit**: ja (nicht reproduziert)

### AUD-012 — MITTEL — Audit-Trail
- siehe AUD-010

### AUD-013 — MITTEL — Edge-Case
- **Datei:Zeile**: server.js (global, 546 Vorkommen)
- **Beschreibung**: Date.now() statt zentralisiertem ClockProvider; localtime in `toDateString()` für DailyLoss
- **Auswirkung**: TZ-Wechsel (Sommer/Winterzeit) kann Tageswechsel inkonsistent triggern
- **Empfehlung**: UTC-only intern, ClockProvider-Modul
- **Freigabe**: nein
- **Unsicherheit**: nein

### AUD-014 — NIEDRIG — UI
- **Datei:Zeile**: public/index.html
- **Beschreibung**: Tab-Mechanismus nicht via data-tab (kein Deep-Linking)
- **Empfehlung**: data-tab + URL-Hash sync
- **Freigabe**: nein

### AUD-015 — NIEDRIG — UI/Accessibility
- **Datei:Zeile**: public/index.html:16
- **Beschreibung**: `user-scalable=no` Accessibility-Anti-Pattern
- **Empfehlung**: entfernen, dafür Touch-Targets vergrößern
- **Freigabe**: nein

### AUD-016 — MITTEL — Critical-Action-Gate
- **Datei:Zeile**: public/index.html:3065
- **Beschreibung**: Trade-Bestätigungs-Modal ohne Re-Auth/PIN
- **Empfehlung**: PIN-Field oder 2FA-Token
- **Freigabe**: ja

### AUD-017 — MITTEL — Tests
- **Datei:Zeile**: workspace
- **Beschreibung**: Keine `tests/` Directory, kein Unit-Test-Framework
- **Auswirkung**: Refactoring riskant
- **Empfehlung**: jest/vitest + Coverage-Pflicht
- **Freigabe**: ja

### AUD-018 — NIEDRIG — Logging
- pm2-logrotate konfigurieren (max_size, retain)
- **Freigabe**: nein

### AUD-019 — NIEDRIG — Edge-Case
- Pidfile-Lock als Defense-in-Depth zu PM2
- **Freigabe**: nein

### AUD-020 — NIEDRIG — Edge-Case
- Float-Drift bei Money (Number statt BigDecimal)
- aktuell `7.4e-14` Drift → vernachlässigbar
- **Freigabe**: nein

---

## PRIORISIERTE FIX-REIHENFOLGE

1. **AUD-001** Auth-Middleware für sensitive Endpoints → KRITISCH, sofort
2. **AUD-002** getEffectiveDemoEquity Doppel-Count + peakTotal-Migration → HOCH, koordinierte Pipeline
3. **AUD-003** clientOrderId/Idempotency-Key vor LIVE-Schaltung
4. **AUD-004** Reserve-Routing für MBT-Profits (Helper-Spec liegt schon)
5. **AUD-016** Re-Auth-Layer für Critical-Action-Modal
6. **AUD-007** Bitget-Lot-Size-Validation vor Order-Send
7. **AUD-005** SCORE_FLOOR-Hybrid auflösen
8. **AUD-013** UTC-Cleanup für DailyLoss-Tageswechsel
9. **AUD-006** + AUD-010 + AUD-012 dedizierte `audit_trail`-Tabelle
10. **AUD-017** Test-Framework etablieren (jest/vitest)

---

## "SCHEINLOGIK"-LISTE (zusammenfassend)

| Feature | Code-Status | Risiko |
|---------|-------------|--------|
| Wyckoff | 0 Vorkommen | Erwartungs-Drift |
| Multi-TF | 0 Vorkommen | Erwartungs-Drift |
| CMO-Indikator | 0 Vorkommen | Erwartungs-Drift |
| PlaceBestExecutionOrder | Hardcoded Stub | Hoch — User könnte glauben es funktioniert |
| LSTM v5 | LogReg-Surrogate (nicht echter LSTM) | Mittel — dir_acc 48.5% < 52% Schwelle |
| Hyperopt | Pure-JS Random+HillClimb (nicht Bayes) | Niedrig — funktional aber sample-inefficient |
| Visual Debugger | 0 Vorkommen | Niedrig |
| Reset Day Zero | Spec-only, kein Code | Mittel — wenn Christian "machen!" sagt funktioniert nichts |
| Wallet-Auto-Routing MBT→Reserve | Spec dokumentiert, nicht implementiert | Hoch (siehe AUD-004) |

---

## "UNSICHERE" STELLEN

| Stelle | Grund |
|--------|-------|
| AUD-009 Funding-Fee in PnL | FundingEngine.js nicht zeilenweise inspiziert — unklar ob Output in Trades.close() landet |
| AUD-011 Bitget-Auth-Stack-Traces | nicht reproduziert, theoretisches Risiko |
| Sub-Bot-Slot Sicherheit | `nexus_sub_1` Konfig vorhanden, aber scripts/sub_bot.js nicht inspiziert |
| isolated-vm ScriptEngine | Z.1424 — sandbox nicht zeilenweise auf Escape-Risiken geprüft |

---

## OFFENE FRAGEN AN VISIONÄR

1. **SCORE_FLOOR_MODE 'log_only'**: Übergang oder Dauer? Wenn Dauer → Code aufräumen. Wenn Übergang → wann active_block?
2. **BRAIN_MODE='voter'**: Soll Brain je auf 'authority' (Final-Decider) wandern? Welche Voraussetzung?
3. **Auth-Layer**: Soll Bot via Tailscale-only bind werden, oder voller Login-Schutz auf Express-Layer? Welcher Pragmatismus?
4. **Reserve-Routing für MBT**: Pipeline-Freigabe für Helper-Aufrufe in Grid/DCA-Trading-Logik? (siehe AUD-004)
5. **Wallet-Display-Fix (X)**: Soll koordinierte Pipeline mit `peakTotal`-Reset folgen? (siehe AUD-002)
6. **Wyckoff/MultiTF/CMO**: Aus Vision-Dokumenten streichen oder bauen?
7. **Tests**: Jest/Vitest etablieren? Welche Coverage-Pflicht?
8. **PlaceBestExecutionOrder**: Stub-Status klar dokumentieren oder bauen?

---

## REPORT-METADATA

- **Audit-Dauer**: ca. 30 Min
- **Modus**: READ-ONLY, keine Code-Änderung, kein Restart, kein Commit
- **Bot währenddessen unverändert**: PM2 R=101 unverändert über gesamte Audit-Dauer ✅
- **Wallet währenddessen unverändert**: 999.024025457343 USDT ✅
- **Vergleichs-Baselines genutzt**: Verify-Audits 17.05. + 18.05. + Kapital-Diagnose 06:30 + Kapital-ATOM-Fix-Pipeline 07:40
